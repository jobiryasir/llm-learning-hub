# Core ML/DL
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.25.0

# Training & Fine-tuning
peft>=0.8.0
trl>=0.8.0
bitsandbytes>=0.43.0
unsloth

# RAG & Vector DB
langchain>=0.1.0
langchain-community
langchain-huggingface
llama-index>=0.10.0
chromadb>=0.4.0
qdrant-client

# Inference & Serving (GPU required - install separately if needed)
# vllm>=0.4.0  # Uncomment if you have CUDA GPU
# llama-cpp-python  # Uncomment for local inference

# Agents
smolagents
crewai

# Evaluation
lm-eval>=0.4.0

# Data & Visualization
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Jupyter & Development
jupyterlab>=4.0.0
ipywidgets>=8.0.0
notebook

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
rich>=13.0.0
pydantic>=2.0.0

# Experiment Tracking (optional)
# wandb
# mlflow
